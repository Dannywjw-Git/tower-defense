# 系统文件
.DS_Store
Thumbs.db
desktop.ini

# 编辑器
.vscode/
.idea/
*.swp

# 临时文件（本地调试产物，不应进仓库）
.tmp_*
*.tmp
*.log

# 依赖（本项目零依赖、无构建，正常不会出现）
node_modules/

# Kenney 素材（已下载但未接入 —— 零代码引用，纳入部署会白涨 3 MB）
# 详见 docs/HANDOFF.md §3 ④「Kenney 素材接入」。
#   · kenney_tower-defense = **等距**版，与正交方格不兼容 → 永久排除
#   · kenney_tower-defense-top-down = **正交**版，可用；但整包 3.86 MB 不进仓库，
#     正式接入时只导出用到的 8 张装饰散图（实测共 10.7 KB）到 assets/
#     见 docs/TERRAIN-PROBE.md「方向 2」
src/kenney_tower-defense/
src/kenney_tower-defense.zip
src/kenney_tower-defense-top-down/
commitmsg.txt

# 探测/临时脚本产物（throwaway，不入正式产物）
docs/screenshots/probe-*.png
docs/screenshots/_*.png
